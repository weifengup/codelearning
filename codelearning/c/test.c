#include<stdio.h>

int main(){
    //f int a,b;
    // scanf("%d,%d",&a,&b);
    // printf("a:%d,b:%d",a,b);
    // enum day{mon,tue,wed,thu};
    // enum day d;
    /*int a;
    char c;
    printf("input a 5-digital:");
    while(scanf("%d",&a)==0||a<100||a>999){
        printf("\ninput a 5-digital:");
        while((c=getchar())!='\n'){
            printf("%c\n",c);
        }
    }
    printf("\na is:%d",a);
    */
//    int arr[20]={1,2,3};
//    printf("%d,%d",sizeof(arr),sizeof(arr)/sizeof(arr[0]));
    int a,b;
    scanf("a=%d,b=%d",&a,&b);
    printf("a:%d,b:%d",a,b);
    return 0;
}